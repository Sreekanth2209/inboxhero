# copy to .env and fill in. config.py reads these; nothing is hardcoded.
MODEL_PROVIDER=gemini
GEMINI_API_KEY=your-key-here
MODEL_NAME=gemini-3.6-flash
# free tier is ~15 requests/min; keep a gap between calls
CALL_GAP_SECONDS=4
